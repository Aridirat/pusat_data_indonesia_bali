<!-- NAVBAR -->
    <header class="bg-stikom rounded-sm shadow p-2 flex justify-end sticky top-0 z-40">
        <div class="text-right py-1 pe-2">
            <p class="font-semibold text-sm text-gray-200">{{ Auth::user()->name }}</p>
            <p class="text-xs text-gray-400">Administrator</p>
        </div>
    </header>